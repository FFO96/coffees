export const textLength = {
  min: 3,
  short: 40,
  medium: 100,
  large: 250,
};

export const regExp = {
  name: /^[a-zA-Z ]*$/,
  description: /^[a-zA-Z .,:;()?!'"]*$/,
};

export enum CoffeeType {
  ARABIC = 'Arabic',
  ROBUSTA = 'Robusta',
}
